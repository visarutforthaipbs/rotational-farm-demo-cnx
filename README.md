# ระบบติดตามและแสดงผลวัฏจักรไร่หมุนเวียน (Rotational Farming Visualization)

โปรเจกต์นี้เป็นเว็บแอปพลิเคชันที่พัฒนาขึ้นเพื่อแสดงข้อมูลและสร้างความเข้าใจเกี่ยวกับ **"ไร่หมุนเวียน"** ในพื้นที่จังหวัดเชียงใหม่ โดยเน้นการนำเสนอข้อมูลเชิงพื้นที่ (Spatial Data) ผ่านแผนที่แบบ Interactive และ Dashboard แสดงสถิติทางนิเวศวิทยา

## 🌟 ฟีเจอร์หลัก (Key Features)

### 1. แผนที่แบบ Interactive (Interactive Map)

- **เทคโนโลยี**: ใช้ [MapLibre GL JS](https://maplibre.org/) ในการเรนเดอร์แผนที่
- **ชั้นข้อมูล (Layers)**:
  - **ภาพถ่ายดาวเทียม**: แสดงสภาพภูมิประเทศจริง
  - **แปลงที่ดิน**: จำแนกสีตามสถานะ (สีเขียว = พื้นที่พักฟื้น/Carbon Sink, สีส้ม = พื้นที่ทำกิน/Active Farm)
  - **ขอบเขตหมู่บ้าน**: แสดงเส้นแบ่งเขตการปกครองระดับหมู่บ้าน
  - **3D Terrain**: แสดงภูมิประเทศแบบ 3 มิติ

### 2. แดชบอร์ดแสดงผลข้อมูล (Data Dashboard)

- **Wong Jorn (วัฏจักรไร่หมุนเวียน)**: แสดงสัดส่วนของ "ป่าฟื้นฟู" เทียบกับพื้นที่ทั้งหมด เพื่อชี้ให้เห็นถึงความยั่งยืนของระบบไร่หมุนเวียน
- **Carbon Counter**: คำนวณและแสดงปริมาณคาร์บอนสะสม (Carbon Stock) ในพื้นที่ที่แสดงผลอยู่
- **Analytics**:
  - **Pie Chart**: สัดส่วนการใช้ที่ดิน (พักฟื้น vs ทำกิน)
  - **Bar Chart**: แสดง 5 อันดับพืชเศรษฐกิจหลักในพื้นที่

### 3. การค้นหาและกรองข้อมูล (Search & Filter)

- ระบบค้นหาหมู่บ้านตามชื่อ
- ตัวกรอง (Filter) เพื่อเลือกดูเฉพาะพื้นที่พักฟื้น หรือ พื้นที่ทำกิน

### 4. การออกแบบที่รองรับทุกอุปกรณ์ (Responsive Design)

- รองรับการใช้งานทั้งบน Desktop, Tablet และ Mobile
- UI ปรับเปลี่ยนรูปแบบอัตโนมัติ (เช่น แผงควบคุมแบบพับเก็บได้บนมือถือ)

## 🛠 เทคโนโลยีที่ใช้ (Tech Stack)

- **Framework**: [Next.js](https://nextjs.org/) (App Router)
- **Language**: TypeScript
- **Styling**: [Tailwind CSS](https://tailwindcss.com/)
- **Map Library**: MapLibre GL JS
- **Charting**: [Recharts](https://recharts.org/)
- **Font**: DB Helvethaica X

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

- `app/`: Source code หลักของ Next.js App Router
  - `page.tsx`: หน้าหลัก
  - `RotationalMap.tsx`: คอมโพเนนต์แผนที่หลัก
  - `Dashboard.tsx`: คอมโพเนนต์แสดงผลข้อมูลสถิติ
- `public/`: ไฟล์ Static assets (รูปภาพ, ฟอนต์)
- `data/`: (หรือไฟล์ json ใน app) ข้อมูล GeoJSON ของแปลงที่ดินและหมู่บ้าน

## 🚀 การติดตั้งและรันโปรเจกต์ (Installation)

1. **Clone Repository**

   ```bash
   git clone https://github.com/visarutforthaipbs/rotational-farm-demo-cnx.git
   cd rotational-farm-demo-cnx
   ```

2. **ติดตั้ง Dependencies**

   ```bash
   npm install
   ```

3. **รันเซิร์ฟเวอร์สำหรับพัฒนา (Development Server)**

   ```bash
   npm run dev
   ```

   เปิดบราวเซอร์ไปที่ [http://localhost:3000](http://localhost:3000)

4. **สร้าง Production Build**
   ```bash
   npm run build
   npm start
   ```

## 📝 ข้อมูลเพิ่มเติม

โปรเจกต์นี้มุ่งเน้นการนำเสนอข้อมูลเพื่อสร้างความเข้าใจที่ถูกต้องเกี่ยวกับวิถีเกษตรแบบไร่หมุนเวียน ซึ่งมีส่วนช่วยในการกักเก็บคาร์บอนและรักษาความหลากหลายทางชีวภาพ
